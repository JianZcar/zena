#!/usr/bin/env bash

BREW_ROOT="/var/home/linuxbrew/"

exec sudo bash -c '
  mkdir -p /var/home/linuxbrew/.cache
  chown -R linuxbrew:linuxbrew "'"$BREW_ROOT"'" >/dev/null 2>&1
  cd "'"$BREW_ROOT"'"
  sudo -u linuxbrew "'"$BREW_ROOT"'/.linuxbrew/bin/brew" "$@"
' bash "$@"
